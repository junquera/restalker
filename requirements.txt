# reStalker - CPU-only Requirements (Default)
# Install with: uv pip install -r requirements.txt
# OR: uv sync (if using pyproject.toml)
#
# This is the default, lightweight installation with CPU-only PyTorch.
# Disk Space: ~500MB
# Performance: Good for most use cases, portable across all systems
#
# For GPU acceleration, use:
#   - NVIDIA GPU: uv pip install -r requirements-gpu-cuda.txt
#   - AMD GPU:    uv pip install -r requirements-gpu-rocm.txt

# Core dependencies
base58
bech32ref
bip-utils
bs4
monero
web3
lxml
gliner2
phonenumbers
python-magic
torch>=2.0.0


